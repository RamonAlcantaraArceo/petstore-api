apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "petstore-api.fullname" . }}-config
  labels:
    {{- include "petstore-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
